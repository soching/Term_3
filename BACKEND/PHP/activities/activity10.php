<?php
    $numbers = [5, 3, 4, 0, 5, 1];

    // Use a WHILE loop to display the numbers until it is a 0 (so display 5, 3, 4 only)
    $number=[5,3,4,0,5,1];
    $index=0;
    while($number[$index] !=0){
        echo $number[$index];
        $index ++ ;
    }
?>

